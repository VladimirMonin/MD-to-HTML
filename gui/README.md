# GUI для MD-to-HTML конвертера

Графический интерфейс на PyQt6 для удобной работы с конвертером Markdown в HTML/EPUB.

## Запуск

```bash
# Установка зависимостей (если еще не установлены)
poetry install

# Или через pip
pip install PyQt6

# Запуск GUI
python gui_app.py
```

## Возможности

### 🔗 Формат исходных ссылок

- **Obsidian** - формат ссылок из Obsidian (`![[image.png]]`)
- **Standard** - стандартный Markdown (`![](image.png)`)

### 📁 Drop-зоны

Перетаскивайте файлы и папки прямо в интерфейс:

1. **Входной файл/папка** - `.md` файл или папка с Markdown файлами
2. **Папка с медиа-файлами** - где искать изображения, видео и другие медиа
3. **Папка вывода** - куда сохранять результаты конвертации

### 📦 Выходные форматы

- ✅ HTML - один файл с встроенными стилями
- ✅ EPUB - электронная книга
- ✅ Оба формата одновременно

### ⚙️ Параметры

**Шаблон:**

- `book` - для книг и документации (с оглавлением)
- `web` - для веб-страниц

**Режим медиа:**

- `embed` - встраивать медиа в HTML (base64)
- `copy` - копировать в отдельную папку `media/`

**Функции:**

- 📑 Оглавление (TOC) - автоматическое содержание
- 🍞 Хлебные крошки - навигация
- 📋 Копирование кода - кнопка копирования в блоках кода
- 📊 Диаграммы Mermaid - поддержка Mermaid
- 💬 Callout блоки - красивые блоки с предупреждениями

### 🎯 Рабочий процесс

#### Временные изменения (только в памяти)

1. Перетащите файлы/папки в drop-зоны
2. Настройте параметры
3. Нажмите **🚀 Сгенерировать**

Настройки применятся только для текущей конвертации и **не изменят** `config.yaml`

#### Сохранение настроек

1. Настройте параметры как нужно
2. Нажмите **💾 Сохранить настройки**

Текущие настройки будут **записаны в `config.yaml`** и загрузятся при следующем запуске

### 📋 Лог операций

В нижней части окна отображается статус всех операций:

- ✅ Успешные действия
- ⚠️ Предупреждения
- ❌ Ошибки
- 📝 Информационные сообщения

## Архитектура

```
gui/
├── __init__.py          # Экспорт модулей
├── main_window.py       # Главное окно приложения
├── widgets.py           # Кастомные виджеты (drop-зоны)
└── config_manager.py    # Управление конфигурацией

gui_app.py               # Точка входа для запуска GUI
```

### Модули

#### `main_window.py`

- `MainWindow` - главное окно с полным интерфейсом
- `ConversionWorker` - фоновый поток для конвертации

#### `widgets.py`

- `DropZone` - базовая drop-зона для файлов/папок
- `InputDropZone` - специализированная зона для входных MD файлов

#### `config_manager.py`

- `ConfigManager` - чтение и запись `config.yaml`
- Поддержка точечной нотации для вложенных ключей

## Особенности реализации

### Управление состоянием

- При запуске загружается `config.yaml`
- Все изменения в GUI сохраняются в `self.current_settings` (в памяти)
- Кнопка "Сохранить" записывает в файл
- Кнопка "Сгенерировать" использует текущее состояние

### Многопоточность

Конвертация выполняется в отдельном потоке (`QThread`), чтобы не блокировать интерфейс

### Темная тема

Полностью кастомная темная тема с акцентом на синий цвет Microsoft

## Требования

- Python 3.9+
- PyQt6 6.6.0+
- PyYAML 6.0+

## Troubleshooting

### PyQt6 не установлен

```bash
poetry add PyQt6
# или
pip install PyQt6
```

### config.yaml не найден

Убедитесь, что запускаете GUI из корневой папки проекта, где находится `config.yaml`

### Конвертация не работает

Проверьте, что все зависимости установлены и `md_converter` доступен
