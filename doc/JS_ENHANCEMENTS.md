# JavaScript Enhancements для Pandoc HTML

## Возможности

### 📋 Копирование кода

- Кнопка "📋" в правом верхнем углу каждого блока кода
- Клик → копирование в буфер обмена
- Визуальная обратная связь (✅ на 2 секунды)

### 🔍 Fullscreen режим

**Изображения:**

- Клик по любому изображению → открытие на весь экран
- Адаптивный размер (fit to screen)
- Hover эффект (увеличение + тень)

**Mermaid диаграммы:**

- Клик по SVG диаграмме → fullscreen
- Увеличенный размер для детального просмотра
- Работает с любыми Mermaid графиками

**Управление:**

- ESC или клик вне элемента → закрытие
- Кнопка "✕" в правом верхнем углу

### 🎯 Smooth Scroll

- Плавная прокрутка при клике на ссылки TOC
- Автоматическая навигация к заголовкам

## Как работает

### Встраивание в HTML

JavaScript вшивается напрямую в HTML через `--include-in-header`:

```python
# В build_book.py
js_content = open("assets/js/pandoc_enhancements.js").read()
header_html = f"<script>{js_content}</script>"
```

### CSS стили

Все стили в `assets/css/book_style.css`:

- `.code-copy-btn` - стили кнопки копирования
- `.fullscreen-container` - overlay контейнер
- `.fullscreen-close-btn` - кнопка закрытия
- Hover эффекты для изображений и диаграмм

### Автоматическая инициализация

```javascript
document.addEventListener("DOMContentLoaded", function () {
  addCodeCopyButtons();      // Добавляет кнопки ко всем pre блокам
  enableFullscreenMedia();    // Навешивает обработчики на img и svg
  smoothScrollTOC();         // Плавная прокрутка для TOC
});
```

## Кастомизация

### Отключить fullscreen для конкретного изображения

```html
<img src="..." class="no-fullscreen">
```

### Изменить иконку кнопки копирования

В `pandoc_enhancements.js`:

```javascript
btn.innerHTML = "📋";  // Измените на любой эмодзи или текст
```

### Изменить цвета/стили

В `book_style.css` редактируйте:

- `.code-copy-btn` - стили кнопки
- `.fullscreen-container` - фон overlay (сейчас rgba(0,0,0,0.95))

## Совместимость

✅ **Работает:**

- Все современные браузеры (Chrome, Firefox, Safari, Edge)
- Мобильные браузеры (iOS Safari, Chrome Mobile)

⚠️ **Не работает в EPUB:**

- JavaScript не выполняется в EPUB readers
- Только для HTML версии

## Примеры использования

### Блок кода

```python
def example():
    pass  # Кнопка копирования появится автоматически
```

### Изображение

```markdown
![Диаграмма](diagram.png)
<!-- Клик → fullscreen -->
```

### Mermaid диаграмма

```mermaid
graph LR
    A --> B
    B --> C
<!-- Клик → fullscreen -->
```

## Расширение функциональности

Добавьте новые функции в `pandoc_enhancements.js`:

```javascript
// Пример: подсветка активного заголовка в TOC
function highlightActiveTOC() {
  // Ваш код
}

// Добавьте в DOMContentLoaded
document.addEventListener("DOMContentLoaded", function () {
  // ... существующие функции
  highlightActiveTOC();  // Новая функция
});
```
