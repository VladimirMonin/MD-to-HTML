# Тест Callouts

## Базовые callouts

> [!note] Это заметка
> Просто информационный блок с текстом.

> [!info] Информация
> Полезная информация для пользователя.

> [!tip] Совет
> Полезный совет как лучше сделать.

> [!warning] Предупреждение
> Будьте осторожны!

> [!danger] Опасность
> Критическая ошибка может произойти!

> [!success] Успех
> Операция выполнена успешно!

> [!question] Вопрос
> Часто задаваемый вопрос?

> [!example] Пример
> Вот пример кода:
>
> ```python
> print("Hello")
> ```

> [!bug] Баг
> Найдена критическая уязвимость!

> [!quote] Цитата
> Великие умы обсуждают идеи.

## Сложные callouts с вложенностью

> [!note] Callout с заголовком h4
> #### Важный подзаголовок внутри
> 
> Текст после заголовка. Давайте проверим что всё работает нормально.

> [!info] Callout с вложенными списками
> 
> Маркированный список:
> - Первый пункт
>   - Вложенный пункт 1
>   - Вложенный пункт 2
>     - Ещё глубже
> - Второй пункт
> 
> Нумерованный список:
> 1. Шаг первый
>    1. Подшаг 1.1
>    2. Подшаг 1.2
> 2. Шаг второй

> [!tip] Callout с однострочным кодом
> 
> Используйте команду `poetry run python cli.py input.md -o output -m copy --no-toc --no-breadcrumbs` для конвертации с параметрами.
> 
> Ещё одна длинная команда: `docker run -it --rm -v $(pwd):/workspace -w /workspace python:3.13-slim bash -c "pip install poetry && poetry install && poetry run pytest"`

> [!example] Callout с многострочным кодом
> 
> Пример Python функции:
> 
> ```python
> def process_markdown(content: str, config: dict) -> tuple[str, dict]:
>     """
>     Обрабатывает Markdown контент.
>     
>     Args:
>         content: Исходный Markdown
>         config: Конфигурация обработки
>     
>     Returns:
>         Обработанный контент и метаданные
>     """
>     media_map = {}
>     for line in content.split('\n'):
>         if line.startswith('!['):
>             media_map[line] = process_media(line)
>     return content, media_map
> ```
> 
> Пример JavaScript:
> 
> ```javascript
> function initBreadcrumbs() {
>   const container = document.querySelector('.breadcrumbs-dynamic');
>   const headers = document.querySelectorAll('h1, h2, h3');
>   
>   headers.forEach((header, index) => {
>     const link = document.createElement('a');
>     link.textContent = header.textContent;
>     link.href = `#${header.id}`;
>     container.appendChild(link);
>   });
> }
> ```

> [!warning] Callout с h4, списками И кодом
> #### ⚠️ Критическое предупреждение
> 
> Перед выполнением следующих действий убедитесь что:
> 
> 1. Создана резервная копия
>    - База данных экспортирована
>    - Файлы скопированы
> 2. Проверены права доступа
> 3. Протестировано на dev-сервере
> 
> Команда для бэкапа:
> ```bash
> tar -czf backup-$(date +%Y%m%d).tar.gz /var/www/html
> pg_dump -U postgres mydb > backup.sql
> ```
> 
> После бэкапа выполните: `sudo systemctl restart nginx`

> [!danger] Комплексный вложенный callout
> #### 🔥 Критическая секция
> 
> Данные операции **необратимы**:
> 
> - Удаление данных
>   - Очистка кеша: `redis-cli FLUSHALL`
>   - Сброс БД: `DROP DATABASE production;`
>   - Удаление файлов:
>     ```bash
>     rm -rf /var/lib/data/*
>     find /tmp -type f -mtime +7 -delete
>     ```
> - Изменение конфигурации
>   1. Остановить сервисы
>   2. Применить изменения
>   3. Запустить сервисы
> 
> Python скрипт для проверки:
> ```python
> import os
> import shutil
> 
> def cleanup_old_files(directory: str, days: int = 7):
>     """Удаляет файлы старше N дней."""
>     for root, dirs, files in os.walk(directory):
>         for file in files:
>             filepath = os.path.join(root, file)
>             if os.path.getmtime(filepath) < time.time() - (days * 86400):
>                 os.remove(filepath)
>                 print(f"Удалён: {filepath}")
> ```
> 
> **Всегда тестируйте на dev окружении!**

