# MCP Server Development Guide: Lessons Learned

## Что такое MCP Server и зачем он нужен

**Model Context Protocol (MCP)** — это протокол для интеграции внешних инструментов с AI-ассистентами (GitHub Copilot, Claude, etc.). MCP сервер предоставляет "tools" (функции), которые AI может вызывать для выполнения задач.

**Для чего это нужно:**

- AI-агент в VS Code может напрямую вызывать ваши инструменты
- Конвертировать файлы, анализировать данные, работать с API
- Всё это происходит автоматически, без копирования кода в чат

**Ключевая особенность:** MCP работает через **stdio transport** (stdin/stdout), что создаёт множество подводных камней.

---

## КРИТИЧЕСКАЯ КОНЦЕПЦИЯ: Чистота stdout

### Проблема №1: JSON-RPC требует чистого stdout

MCP использует JSON-RPC протокол через stdio:

- Клиент (VS Code) отправляет JSON в stdin сервера
- Сервер отвечает JSON в stdout
- **ЛЮБОЙ** не-JSON текст в stdout ЛОМАЕТ протокол

**Что ломает MCP сервер:**

```
print("Starting...") → stdout pollution → протокол сломан
logger.info("Processing") → если логгер пишет в stdout → сломан
subprocess без redirect → может писать в stdout → сломан
```

**ЗОЛОТОЕ ПРАВИЛО:** Весь debug/info вывод ТОЛЬКО в `stderr`, никогда в `stdout`.

---

## Правильное логирование для MCP

### Базовая настройка (в начале mcp_server.py)

```python
import sys
import logging
from pathlib import Path

# Файловое логирование
LOG_FILE = Path(__file__).parent / "mcp_debug.log"
file_handler = logging.FileHandler(LOG_FILE, mode='a', encoding='utf-8')
file_handler.setFormatter(logging.Formatter('%(asctime)s - %(levelname)s - %(message)s'))

logger = logging.getLogger('mcp_server')
logger.setLevel(logging.DEBUG)
logger.addHandler(file_handler)
logger.propagate = False  # КРИТИЧНО! Не передавать в root logger

def log_debug(message: str):
    """Логирование в файл И stderr (для VS Code Output)."""
    logger.debug(message)  # В файл
    print(f"[MCP] {message}", file=sys.stderr)  # В stderr для VS Code
```

**Почему это важно:**

- `file=sys.stderr` — вывод виден в VS Code Output
- `logger.debug()` — постоянная запись в файл для посмертного анализа
- `logger.propagate = False` — предотвращает случайный вывод в stdout

**Где логировать:**

- Начало/конец операции
- Каждый шаг длинной обработки (для прогресс-индикации)
- ДО и ПОСЛЕ subprocess вызовов
- Все ошибки с полным traceback

---

## Subprocess: Главная причина зависаний

### Проблема №2: subprocess блокирует MCP stdio

Когда MCP сервер запускает `subprocess.run()`:

- Дочерний процесс **наследует** stdin/stdout/stderr родителя
- Если дочерний процесс пытается читать stdin → **зависание навсегда**
- MCP клиент ждёт ответ → **timeout через 30 минут**

**ОБЯЗАТЕЛЬНО во ВСЕХ subprocess вызовах:**

```python
subprocess.run(
    cmd,
    stdin=subprocess.DEVNULL,  # ← КРИТИЧНО! Закрыть stdin
    capture_output=True,       # Перехватить stdout/stderr
    text=True,
    timeout=30,               # Таймаут обязателен
)
```

**Почему `stdin=subprocess.DEVNULL` обязателен:**

- Без него процесс может попытаться читать stdin
- В MCP stdio транспорте stdin занят JSON-RPC протоколом
- Попытка чтения = deadlock

**Примеры проблемных инструментов:**

- Mermaid CLI (mmdc) — рендеринг диаграмм
- Pandoc — конвертация документов
- Node.js скрипты
- Любой интерактивный CLI

---

## Обработка ошибок: Traceback обязателен

### Проблема №3: Непонятные ошибки без traceback

Стандартный `str(e)` даёт только текст ошибки:

```
"[WinError 32] Процесс не может получить доступ к файлу"
```

**Где произошла ошибка? В каком файле? На какой строке?** — непонятно.

**Правильная обработка:**

```python
except Exception as e:
    import traceback
    tb_str = traceback.format_exc()
    log_debug(f"ОШИБКА: {type(e).__name__}: {e}\n{tb_str}")
    
    return {
        "status": "error",
        "error_type": type(e).__name__,
        "message": str(e),
        "details": {
            "traceback": tb_str  # Полный стек вызовов
        }
    }
```

**Что это даёт:**

- Точное место ошибки в коде
- Весь стек вызовов
- Значения переменных (если нужно)
- Быстрая диагностика

---

## Двойная обработка: Производительность убийца

### Проблема №4: Валидация + Обработка = 2× времени

**Сценарий:** У нас есть 50 Mermaid диаграмм для рендеринга.

**Плохая архитектура:**

1. `validate_mermaid_blocks()` — запускает mmdc для каждой (50 × 1.5 сек = 75 сек)
2. `MermaidPreprocessor.process()` — рендерит каждую снова (50 × 1.5 сек = 75 сек)
3. **Итого:** 150 секунд вместо 75

**Решение:**

- Валидация должна быть **опциональной** с `default=False`
- Или валидация должна **кэшировать** результаты
- Или валидация и обработка должны быть **одним шагом**

**Правильный default в tool parameters:**

```python
@mcp.tool()
def convert_markdown(
    input_file: str,
    validate_mermaid: bool = False,  # ← False по умолчанию!
):
```

**Почему False:**

- Валидация нужна только при разработке/отладке
- В production лишняя работа
- Пользователь может включить явно если нужно

---

## Временные файлы: Windows PermissionError

### Проблема №5: Файлы заняты другим процессом

**Windows блокирует файлы агрессивнее чем Linux:**

- Открытый файл нельзя удалить
- PIL Image держит файл открытым
- subprocess может оставлять lock файлы

**Неправильно:**

```python
png_image = Image.open("file.png")
png_image.save(buffer, "WEBP")
# ← файл ВСЁ ЕЩЁ открыт!
Path("file.png").unlink()  # PermissionError!
```

**Правильно:**

```python
# Используем context manager
with Image.open("file.png") as png_image:
    png_image.save(buffer, "WEBP")
# ← файл закрыт автоматически

# Даём Windows время освободить файл
import time
time.sleep(0.05)  # 50ms

# Удаление с обработкой ошибок
try:
    Path("file.png").unlink()
except PermissionError:
    pass  # Windows удалит позже
```

**Ключевые правила:**

- Всегда используйте `with` для открытия файлов
- Добавляйте небольшую задержку перед удалением
- Обрабатывайте PermissionError gracefully

---

## Копирование файлов: Проверка источника/назначения

### Проблема №6: Копирование файла самого в себя

**Сценарий:**

1. MermaidPreprocessor создаёт `output_dir/media/diagram_1.webp`
2. Markdown теперь содержит `![](media/diagram_1.webp)`
3. MediaProcessor находит файл и пытается копировать его в... `output_dir/media/`
4. **shutil.copy2(file, file) → PermissionError!**

**Решение — проверка перед копированием:**

```python
source = Path("/path/to/output/media/diagram.webp")
target = Path("/path/to/output/media/diagram.webp")

# Проверяем что это НЕ один и тот же файл
if source.resolve() == target.resolve():
    # Файл уже там где нужно, пропускаем
    pass
else:
    shutil.copy2(source, target)
```

**Используйте `.resolve()`:**

- Разрешает символические ссылки
- Нормализует путь (убирает `..` и `.`)
- Даёт каноническое имя для сравнения

---

## Прогресс-индикация: Обязательна для долгих операций

### Проблема №7: Молчание сервера = зависание?

**Когда обрабатываются 50 диаграмм:**

- Без логов: тишина 2 минуты → пользователь думает что зависло
- С логами: видно что работает → спокойствие

**Правильная прогресс-индикация:**

```python
# В начале процесса
total = len(diagrams)
log_debug(f"Найдено {total} Mermaid диаграмм")

# В цикле обработки
for idx, diagram in enumerate(diagrams, start=1):
    log_debug(f"Рендеринг диаграммы {idx}/{total}...")
    # ... обработка ...
    
log_debug("Все диаграммы обработаны")
```

**Где это критично:**

- Любая операция >5 секунд
- Batch обработка (много элементов)
- Network запросы
- Subprocess вызовы

---

## Конфигурация MCP в VS Code

**Файл:** `.vscode/settings.json` или `%APPDATA%/Code/User/globalStorage/github.copilot-chat/settings.json`

```json
{
  "mcpServers": {
    "your-server-name": {
      "type": "stdio",
      "command": "C:\\path\\to\\python.exe",
      "args": ["C:\\path\\to\\mcp_server.py"],
      "env": {
        "PYTHONIOENCODING": "utf-8",
        "PYTHONUTF8": "1"
      },
      "autoApprove": ["tool_name_1", "tool_name_2"],
      "disabled": false,
      "timeout": 1800  // 30 минут
    }
  }
}
```

**Важные параметры:**

1. **type: "stdio"** — обязателен для Python серверов
2. **command** — АБСОЛЮТНЫЙ путь к Python интерпретатору
3. **args** — АБСОЛЮТНЫЙ путь к mcp_server.py
4. **env.PYTHONIOENCODING** — правильная кодировка stdout/stderr
5. **autoApprove** — список tools без подтверждения
6. **timeout** — увеличить для долгих операций (default 300 сек)

---

## FastMCP: Создание инструментов

### Основная структура сервера

```python
from mcp.server.fastmcp import FastMCP

mcp = FastMCP(
    "Your Server Name",
    website_url="https://github.com/user/repo"
)

@mcp.tool()
def your_tool(
    param1: str,
    param2: int = 10,  # default значение
) -> dict:
    """
    Описание инструмента (показывается AI).
    
    Args:
        param1: Описание параметра
        param2: Опциональный параметр
        
    Returns:
        dict: Результат операции
    """
    try:
        log_debug(f"Начало: {param1}")
        
        # ... ваша логика ...
        
        log_debug("Завершено успешно")
        return {"status": "success", "result": "..."}
        
    except Exception as e:
        import traceback
        tb_str = traceback.format_exc()
        log_debug(f"ОШИБКА: {e}\n{tb_str}")
        
        return {
            "status": "error",
            "error_type": type(e).__name__,
            "message": str(e),
            "details": {"traceback": tb_str}
        }

if __name__ == "__main__":
    mcp.run()  # Запуск stdio транспорта
```

### Правила для tool функций

1. **Всегда возвращайте dict** — FastMCP автоматически сериализует в JSON
2. **Обрабатывайте все исключения** — иначе сервер упадёт
3. **Логируйте начало/конец** — для отладки
4. **Используйте type hints** — AI видит типы параметров
5. **Пишите docstring** — AI читает описание для выбора tool

---

## Типичные ошибки и их решения

### 1. "Server not responding" / Timeout

**Причины:**

- Subprocess без `stdin=subprocess.DEVNULL`
- Бесконечный цикл в коде
- Deadlock при работе с файлами
- Забыли `return` результат

**Диагностика:**

- Проверьте `mcp_debug.log` — где остановился?
- Добавьте логи в каждый шаг
- Запустите сервер напрямую (не через VS Code)

### 2. "Invalid JSON response"

**Причины:**

- Что-то пишет в stdout (print без file=sys.stderr)
- logger пишет в stdout (не настроен propagate=False)
- subprocess пишет в stdout (не используется capture_output=True)

**Диагностика:**

- Поищите все `print()` без `file=sys.stderr`
- Проверьте настройку logger
- Добавьте `capture_output=True` во все subprocess

### 3. PermissionError / "File in use"

**Причины:**

- Файл не закрыт (забыли `with` или `.close()`)
- Попытка удалить файл сразу после записи (Windows)
- Копирование файла самого в себя

**Решение:**

- Используйте `with` для всех файлов
- Добавьте `time.sleep(0.05)` перед удалением
- Проверяйте `source.resolve() == target.resolve()`

### 4. Двойное логирование ("каждая строка дважды")

**Причины:**

- `logger.propagate = True` (default)
- Root logger имеет свой handler в stdout

**Решение:**

- Всегда ставьте `logger.propagate = False`
- Не используйте `logging.basicConfig()` в MCP сервере

---

## Паттерны для разных типов операций

### Batch обработка с прогрессом

```python
items = [...]  # Список элементов для обработки
total = len(items)
log_debug(f"Обработка {total} элементов")

for idx, item in enumerate(items, start=1):
    log_debug(f"Элемент {idx}/{total}: {item.name}")
    
    # Обработка элемента
    result = process_item(item)
    
    # Логируем промежуточный результат если нужно
    if result.has_errors:
        log_debug(f"  ⚠️ Предупреждение: {result.error}")

log_debug(f"Обработано {total} элементов")
```

### Subprocess с таймаутом и логами

```python
log_debug(f"Запуск: {cmd[0]} с аргументами {cmd[1:]}")

try:
    result = subprocess.run(
        cmd,
        stdin=subprocess.DEVNULL,
        capture_output=True,
        text=True,
        timeout=30,
        check=True,
    )
    log_debug(f"Завершено успешно: {len(result.stdout)} байт вывода")
    
except subprocess.TimeoutExpired:
    log_debug(f"TIMEOUT: процесс не завершился за 30 сек")
    raise
    
except subprocess.CalledProcessError as e:
    log_debug(f"ОШИБКА процесса: {e.stderr}")
    raise
```

### Работа с временными файлами

```python
import tempfile

# Создаём временный файл
with tempfile.NamedTemporaryFile(
    mode="w",
    suffix=".tmp",
    delete=False,  # Не удалять автоматически
    encoding="utf-8"
) as tmp:
    tmp.write(content)
    tmp_path = Path(tmp.name)

try:
    # Используем временный файл
    process_file(tmp_path)
    
finally:
    # Удаляем с обработкой ошибок
    time.sleep(0.05)  # Windows: дать время закрыть
    try:
        tmp_path.unlink()
    except PermissionError:
        log_debug(f"Не удалось удалить {tmp_path}, будет удалён позже")
```

---

## Тестирование MCP сервера

### Прямой запуск (без VS Code)

Создайте test скрипт:

```python
import json
import subprocess
import sys

def test_mcp_tool():
    """Тест MCP сервера напрямую через stdio."""
    
    # Запускаем сервер
    proc = subprocess.Popen(
        [sys.executable, "mcp_server.py"],
        stdin=subprocess.PIPE,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        text=True,
        encoding='utf-8'
    )
    
    # Отправляем initialize
    init_request = {
        "jsonrpc": "2.0",
        "id": 0,
        "method": "initialize",
        "params": {
            "protocolVersion": "2024-11-05",
            "capabilities": {}
        }
    }
    proc.stdin.write(json.dumps(init_request) + "\n")
    proc.stdin.flush()
    
    # Читаем ответ
    response = proc.stdout.readline()
    print(f"Initialize: {response[:100]}...")
    
    # Отправляем вызов tool
    tool_request = {
        "jsonrpc": "2.0",
        "id": 1,
        "method": "tools/call",
        "params": {
            "name": "your_tool_name",
            "arguments": {
                "param1": "test",
                "param2": 42
            }
        }
    }
    proc.stdin.write(json.dumps(tool_request) + "\n")
    proc.stdin.flush()
    
    # Читаем результат
    response = proc.stdout.readline()
    result = json.loads(response)
    print(f"Result: {json.dumps(result, indent=2)}")
    
    # Проверяем stderr (логи)
    proc.terminate()
    stderr = proc.stderr.read()
    print(f"\nЛоги:\n{stderr}")

if __name__ == "__main__":
    test_mcp_tool()
```

### Эмуляция MCP протокола

Для полной эмуляции с таймаутами:

```python
import threading

def read_with_timeout(stream, timeout=60):
    """Чтение из stream с таймаутом."""
    result = [None]
    
    def reader():
        result[0] = stream.readline()
    
    thread = threading.Thread(target=reader, daemon=True)
    thread.start()
    thread.join(timeout)
    
    if thread.is_alive():
        raise TimeoutError(f"Чтение не завершилось за {timeout} сек")
    
    return result[0]
```

---

## Отладка: Где искать проблемы

### 1. VS Code Output Panel

**Как открыть:** View → Output → GitHub Copilot Chat

**Что там видно:**

- `[server stderr]` — всё из sys.stderr вашего сервера
- `Processing request` — когда AI вызывает tool
- Ошибки протокола

**Паттерн для grep:**

```
[warning] [server stderr] [MCP] Ваше сообщение
```

### 2. Лог-файл (mcp_debug.log)

**Когда использовать:**

- Сервер упал/завис
- Нужен полный history операций
- Анализ производительности

**Просмотр последних строк:**

```bash
# Windows PowerShell
Get-Content mcp_debug.log -Tail 50

# Linux/Mac
tail -f mcp_debug.log  # follow режим
```

### 3. Прямой запуск в терминале

**Когда использовать:**

- Нужно видеть все stdout/stderr
- Отладка startup проблем
- Проверка импортов/зависимостей

**Запуск:**

```bash
python mcp_server.py
# Вводим JSON requests руками
# Видим все выводы напрямую
```

---

## Производительность: Что оптимизировать

### 1. Убрать лишние проверки в production

**Пример:** Валидация синтаксиса перед обработкой

- Разработка: `validate=True` (находить ошибки рано)
- Production: `validate=False` (экономить время)

**Решение:** `validate: bool = False` в параметрах tool

### 2. Кэширование повторяющихся операций

Если обрабатываете один и тот же файл несколько раз:

```python
_cache = {}

def expensive_operation(file_path):
    key = f"{file_path}:{Path(file_path).stat().st_mtime}"
    
    if key in _cache:
        log_debug(f"Используем кэш для {file_path}")
        return _cache[key]
    
    result = really_expensive_work(file_path)
    _cache[key] = result
    return result
```

### 3. Параллелизация (осторожно!)

Для независимых операций можно использовать ThreadPoolExecutor:

```python
from concurrent.futures import ThreadPoolExecutor

with ThreadPoolExecutor(max_workers=4) as executor:
    futures = [
        executor.submit(process_item, item)
        for item in items
    ]
    
    for future in futures:
        result = future.result()
```

**НО:** добавляет сложность в отладке и логировании!

---

## Чеклист перед деплоем MCP сервера

### Обязательные проверки

- [ ] Все subprocess используют `stdin=subprocess.DEVNULL`
- [ ] Все subprocess имеют `timeout`
- [ ] Нет `print()` без `file=sys.stderr`
- [ ] Logger настроен с `propagate=False`
- [ ] Файловое логирование включено
- [ ] Все исключения ловятся с полным traceback
- [ ] Есть прогресс-логи для операций >5 сек
- [ ] Временные файлы открываются через `with`
- [ ] Проверка source == target перед копированием
- [ ] Tool defaults подходят для production
- [ ] Есть тест-скрипт для прямого запуска
- [ ] Timeout в VS Code конфиге >= время самой долгой операции

### Рекомендуемые проверки

- [ ] Кэширование для повторяющихся операций
- [ ] Graceful degradation при недоступности внешних сервисов
- [ ] Rate limiting для API вызовов
- [ ] Метрики производительности (timing логи)
- [ ] Автоматическое удаление старых логов

---

## Частые вопросы

### Q: Почему сервер работает в терминале, но не через VS Code?

**A:** Разные окружения:

- Терминал: ваш PATH, переменные окружения
- VS Code MCP: изолированный процесс, только то что в `env` конфига

**Решение:** Используйте абсолютные пути везде, добавьте нужные ENV переменные в конфиг.

### Q: Как дебажить если сервер молчит (нет логов)?

**A:** Проблема в самом начале (import/startup error):

1. Запустите `python mcp_server.py` в терминале
2. Смотрите traceback
3. Обычно это missing dependency или syntax error

### Q: Сервер зависает на первом же вызове tool

**A:** Скорее всего subprocess без `stdin=subprocess.DEVNULL`:

1. Найдите все `subprocess.run()` / `subprocess.Popen()`
2. Добавьте `stdin=subprocess.DEVNULL` везде
3. Перезапустите сервер

### Q: Как увеличить timeout для долгих операций?

**A:** Два места:

1. В конфиге VS Code: `"timeout": 1800` (секунды)
2. В subprocess: `timeout=300` (секунды)

### Q: Логи засоряют диск, как ограничить размер?

**A:** Используйте RotatingFileHandler:

```python
from logging.handlers import RotatingFileHandler

handler = RotatingFileHandler(
    LOG_FILE,
    maxBytes=10*1024*1024,  # 10 MB
    backupCount=5,          # 5 файлов
    encoding='utf-8'
)
```

---

## Итоговые рекомендации

### Что делать ОБЯЗАТЕЛЬНО

1. **Логируйте всё в stderr** — это единственный способ видеть что происходит
2. **Добавляйте stdin=DEVNULL** — без этого будут зависания
3. **Обрабатывайте все исключения** — с полным traceback
4. **Тестируйте напрямую** — не только через VS Code

### Что делать ЖЕЛАТЕЛЬНО

1. Файловое логирование для post-mortem анализа
2. Прогресс-индикация для долгих операций
3. Правильные defaults (False для валидаций)
4. Кэширование повторяющихся операций

### Чего НЕ делать НИКОГДА

1. `print()` без `file=sys.stderr` — ломает протокол
2. subprocess без `stdin=subprocess.DEVNULL` — зависание
3. Игнорировать PermissionError — Windows проблемы
4. Двойная обработка данных — убивает производительность

---

## Заключение

MCP — мощный инструмент для интеграции с AI, но требует понимания stdio транспорта и его ограничений. Главные уроки:

- **Чистота stdout критична** — малейшее загрязнение ломает протокол
- **Subprocess требует особого внимания** — легко создать deadlock
- **Логирование = спасение** — без него отладка невозможна
- **Тестирование вне VS Code** — необходимо для надёжности

Следуя этим правилам, вы создадите стабильный MCP сервер с первой попытки, без часов отладки.

**Удачи в разработке!** 🚀
